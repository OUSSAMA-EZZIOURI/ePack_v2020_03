CREATE TABLE "base_engine_label" (
	"id" INTEGER NOT NULL,
	"create_id" INTEGER NULL DEFAULT NULL,
	"create_time" TIMESTAMP NULL DEFAULT NULL,
	"harness_id" INTEGER NULL DEFAULT NULL,
	"lable_code" VARCHAR(255) NULL DEFAULT NULL,
	"write_id" INTEGER NULL DEFAULT NULL,
	"write_time" TIMESTAMP NULL DEFAULT NULL,
	PRIMARY KEY ("id")
)
;
COMMENT ON COLUMN "base_engine_label"."id" IS '';
COMMENT ON COLUMN "base_engine_label"."create_id" IS '';
COMMENT ON COLUMN "base_engine_label"."create_time" IS '';
COMMENT ON COLUMN "base_engine_label"."harness_id" IS '';
COMMENT ON COLUMN "base_engine_label"."lable_code" IS '';
COMMENT ON COLUMN "base_engine_label"."write_id" IS '';
COMMENT ON COLUMN "base_engine_label"."write_time" IS '';

